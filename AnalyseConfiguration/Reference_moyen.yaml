R34:
  description: "Disable service accounts (non-exhaustive reference list)"
  expected:
    disabled_service_accounts:
      - "www-data"
      - "named"
      - "postgres"
      - "mysql"
      - "backup"
      - "lp"
      - "irc"
      - "games"
      - "nobody"
      - "mail"
      - "systemd-network"
      - "proxy"
      - "tcpdump"
      - "syslog"

R39:
  description: "Modify sudo configuration directives"
  expected:
    sudo_directives:
      - "Defaults noexec"
      - "Defaults requiretty"
      - "Defaults use_pty"
      - "Defaults umask=0027"
      - "Defaults ignore_dot"
      - "Defaults env_reset"

R40:
  description: "Use non-privileged target users for sudo commands"
  expected: []


R42:
  description: "Ban negations in sudo specifications"
  expected:
    no_negation_in_sudo: []

R43:
  description: "Specify arguments in sudo specifications"
  expected:
    strict_sudo_arguments: []

R44:
  description: "Edit files securely with sudo"
  expected:
    sudoedit_only: []

R50:
  description: "Restrict access rights to sensitive files and directories"
  expected:
    secure_permissions:
      - "/etc/shadow 600"
      - "/etc/passwd 644"
      - "/etc/group 644"
      - "/etc/gshadow 600"
      - "/etc/ssh/sshd_config 600"
      - "/root/ 700"
      - "/var/log/auth.log 640"
      - "/var/log/syslog 640"
      - "/var/log/secure 640"
      - "/etc/cron.d 750"
      - "/etc/cron.daily 750"
      - "/etc/cron.hourly 750"
      - "/etc/cron.monthly 750"
      - "/etc/cron.weekly 750"
      - "/etc/fstab 644"
      - "/etc/securetty 600"
      - "/etc/security/limits.conf 644"
      - "/boot/grub/grub.cfg 600"
R52:
  description: "Restrict access to sockets and named pipes"
  expected:
    protected_sockets:
      - "/dev/log"
      - "/run/systemd/journal/socket"
      - "/var/run/docker.sock"
      - "/run/dbus/system_bus_socket"
      - "/run/udev/control"
      - "/var/run/mysqld/mysqld.sock"
      - "/var/run/postgresql/.s.PGSQL.5432"
      - "/var/run/php/php-fpm.sock"
      - "/run/systemd/private"
      - "/run/snapd.socket"
      - "/run/acpid.socket"
      - "/run/cups/cups.sock"
      - "/var/lib/lxcfs/cgroup"
      - "/run/systemd/notify"

R55:
  description: "Separate users' temporary directories"
  expected:
    user_private_tmp: []
